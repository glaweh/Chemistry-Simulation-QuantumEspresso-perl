#!/usr/bin/perl
use strict;
use warnings;
use Chemistry::Simulation::QuantumEspresso::Pw;
use Data::Dumper;

my $data=Chemistry::Simulation::QuantumEspresso::Pw::parse($ARGV[0]);

print Dumper($data);
exit(0);
